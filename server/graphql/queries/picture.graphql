enum PictureType {
    POST,
    USER,
    DOCUMENTARY
}

type Picture {
    _id: ID!
    type: PictureType!
    post: Post
    user: User
    path: String!
    createdAt: DateTime!
    updatedAt: DateTime!
}