enum UserPermissions {
    SUPER_USER,
    MANAGER,
    AUTHOR,
    MEMBER
}
type User {
    _id: ID!
    username: String!
    email: String!
    is_active: Boolean
    permissions: [UserPermissions]!
    picture: Picture!
    posts: [Post!]
    createdAt: DateTime!
    updatedAt: DateTime!
}